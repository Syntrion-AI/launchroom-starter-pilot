# Legacy localized entrypoint

This path is kept as a short compatibility shim for older tests that used `RUN_ME_FIRST_RU.md`.

Use the canonical setup route instead:

```text
https://raw.githubusercontent.com/Syntrion-AI/launchroom-starter-pilot/main/RUN_ME_FIRST.md
```

The agent must detect and mirror the user's language during conversation. Repository documentation and machine contracts remain English.
